import React from "react";
import TextImages from "../TextImages"

export default function ChapterText1_3() {
    return (
        <TextImages>
            {`(iii. canto de aves)
todo sigue, incluso él sentado allí, incluso 
el intentando encontrar sentido a la espera.`}
        </TextImages>
    );
}